import 'normalize.css/normalize.css'; // v4.0.0: missing `main` in package.json
import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';

ReactDOM.render(<App />, document.getElementById('root'));
